{
    "name": "docs",
    "version": "0.0.0",
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "vitepress dev",
        "docs:dev": "vitepress dev",
        "build": "bun scripts/generate-llms.ts && vitepress build",
        "preview": "vitepress preview",
        "lint": "oxlint --deny-warnings"
    },
    "devDependencies": {
        "config": "file:./config",
        "oxlint": "^1.69.0",
        "vitepress": "^1.6.3",
        "vue": "^3.5.13"
    }
}
